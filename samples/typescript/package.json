{
  "name": "simple-mcp-oauth",
  "version": "0.0.1",
  "description": "Simple MCP Server with OAuth Token Exchange",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
