[project]
name = "simple-mcp-oauth"
version = "0.0.1"
description = "Simple MCP Server with OAuth Token Exchange"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.4.2",
    "httpx>=0.28.1",
    "pyjwt>=2.13.0",
]
